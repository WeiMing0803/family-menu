# Family Menu 本地运行与交付约定

## 本地运行

在仓库根目录执行：

    dotnet run --project backend/FamilyMenu.Api/FamilyMenu.Api.csproj --launch-profile http

开发环境默认监听 http://localhost:5080，数据库文件为
backend/FamilyMenu.Api/family.db。该文件只用于本地开发，已经被 .gitignore 忽略。

Development 环境允许使用 POST /api/auth/login 的 openId 字段模拟微信登录，便于接口联调。该回退只在 Development 环境且
Wechat:AllowDevelopmentOpenId=true 时有效，生产环境不会接受客户端传入的 openId。

## 微信登录配置

生产环境必须配置：

- Wechat__AppId
- Wechat__AppSecret
- Jwt__Secret：至少 32 个字符的随机值
- Cors__AllowedOrigins__0：前端 origin，例如 https://menu.example.com
- ForwardedHeaders__KnownProxies__0：反向代理的 IP，例如 127.0.0.1

推荐通过部署平台的环境变量或 Secret 管理功能注入，不要把真实值写入
appsettings.json、appsettings.*.json、日志或 Git。

小程序登录流程是：客户端调用 wx.login 获取一次性 code，请求
POST /api/auth/login，服务端向微信 jscode2session 交换 openid，然后返回 JWT。
服务端对微信请求设置了 5 秒超时，不会把 code、AppSecret、openid 或 JWT 写入日志。

## 生产运行

生产环境要求 HTTPS。通常由 Caddy、Nginx 或云托管入口终止 TLS，再转发到 ASP.NET Core。
如果使用反向代理，必须把代理 IP 配置到 ForwardedHeaders__KnownProxies__0 等环境变量，
应用才会信任该代理转发的 HTTPS 协议头。
应用会启用 HTTPS 重定向、HSTS、安全响应头和受限 CORS；未配置 JWT、微信或 CORS 参数时会拒绝启动。

生产发布前至少检查：

1. Jwt__Secret 是随机密钥，不是示例值。
2. Wechat__AppId 和 Wechat__AppSecret 来自目标小程序。
3. CORS 只包含实际前端 origin，不使用 *。
4. 反向代理正确转发 HTTPS，并限制应用端口的公网访问。
5. SQLite 文件已备份，且不包含在发布包或 Git 中。

## 验证命令

    dotnet build backend/FamilyMenu.Backend.sln --no-restore

健康检查地址：GET /health
